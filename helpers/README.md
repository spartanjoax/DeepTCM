# helpers/

Utility functions shared across all training scripts. Contains two modules: one for signal preprocessing and one for the training loop itself.

## Files

| File | Description |
|---|---|
| `preprocessing.py` | Signal preprocessing transforms applied before windowing and normalisation. |
| `helper.py` | Training loop utilities, metric computation, logging helpers, and plotting. |
